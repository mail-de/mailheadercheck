name: Markdown lint

on:
  push:
    branches: ["**"]
  pull_request:

jobs:
  run_markdownlint:
    name: Run markdownlint
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Run markdownlint
        uses: actionshub/markdownlint@main
